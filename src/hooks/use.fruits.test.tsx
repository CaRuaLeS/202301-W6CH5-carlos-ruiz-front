describe("Given the App component", () => {
  const mockFruitsRepo = {
    readFruits: jest.fn(),
    readOneFruits: jest.fn(),
    create: jest.fn(),
    update: jest.fn(),
    delete: jest.fn(),
  };
  describe("When it renders", () => {
    test("Then it is in the document", () => {});
  });
});
